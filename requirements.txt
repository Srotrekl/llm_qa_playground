# Core test framework
pytest==8.3.4
pytest-asyncio==0.24.0
allure-pytest==2.13.5

# HTTP client
httpx==0.28.1

# Data models
pydantic==2.10.4
pydantic-settings==2.14.2

# Linting (used in CI, installed in dev)
ruff==0.9.1

# Utilities
python-dotenv==1.0.1
